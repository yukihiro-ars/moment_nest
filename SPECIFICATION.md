# NAS写真・動画整理ツール 仕様書（v1.0）

## 1. 目的・背景

### 開発動機
- 家族でスマホ等で撮った写真をNASに保存（スマホ容量確保）
- ファイル名がスマホ依存で、撮影日時が不明
- 体系的に整理したい

### 目標
- **Phase 1（v1.0）**: 撮影日時ベースでファイルを整理・リネーム
- **Phase 2（将来）**: Web閲覧機能
- **Phase 3（将来）**: AIタグ付け機能

---

## 2. システム構成

### 実行環境
- **実行場所**: PCローカルから実行
- **NAS**: Buffalo製、SMB/CIFS接続
- **対象規模**: 将来的に数万ファイル
- **実行方式**: 対話モード（段階的な問い合わせ形式）

### ディレクトリ構成
```
【NAS側】
Z:/Photos/
├── source/              # 整理前の元ファイル（ユーザーが配置）
│   ├── IMG_1234.jpg
│   ├── VID_5678.mp4
│   └── ...
│
└── organized/           # 整理後（プログラムが配置）
    ├── .photo-organizer.lock    # ロックファイル
    ├── 2023/
    ├── 2024/
    │   ├── 20241215143022-01.jpg
    │   ├── 20241215143022-02.mp4
    │   └── ...
    └── 2025/

【PCローカル】
C:/photo-organizer/
├── organize.py          # メインスクリプト
├── config.json          # 設定ファイル
├── requirements.txt     # 依存ライブラリ
│
├── modules/
│   ├── __init__.py
│   ├── scanner.py       # スキャン・分析
│   ├── planner.py       # 整理計画
│   ├── organizer.py     # 実行
│   ├── cleaner.py       # クリーンアップ
│   ├── metadata.py      # メタデータ抽出
│   ├── network.py       # NASハンドラー
│   ├── lock.py          # ロック管理
│   └── utils.py         # ユーティリティ
│
├── checkpoints/         # チェックポイントファイル
│   ├── phase1a.json
│   └── phase1b.json
│
└── reports/             # レポート出力先
    ├── metadata_report.json
    ├── hash_report.json
    ├── analysis_report.json
    ├── duplicates.txt
    ├── organize_plan.txt
    └── organize_log.json
```

---

## 3. ファイル名規則

### 形式
```
YYYYMMDDhhmmss-NN.ext

例:
20241215143022-01.jpg  # 2024年12月15日 14:30:22に撮影
20241215143022-02.jpg  # 同じ秒に撮影された2枚目（連写等）
20241215143023-01.mp4  # 次の秒の動画
```

### 設計方針
- **ファイル名が真実の情報源**（DBなしでも日付がわかる）
- 撮影日時のみ（デバイス名等は含めない）
- 同一秒内の連番で重複回避

### 連番の決定ロジック
```
2回目以降の実行時:
1. organized/ に既存ファイルをスキャン
2. 同じ撮影日時のファイルがあれば、連番を継続
   例: 既存が -01, -02 なら、新規は -03 から開始
```

---

## 4. 撮影日時の取得ロジック

### 優先順位
```
1. EXIF DateTimeOriginal (画像ファイル)
2. 動画メタデータ creation_time
3. EXIF DateTime (上記がない場合)
4. ファイル作成日時 (最終フォールバック)
```

### 注意点
- EXIF情報がない場合でもエラーにせず、ファイル作成日時で処理
- どの情報源を使ったかをレポートに記録

---

## 5. 対応ファイル形式

### 画像
- JPEG (`.jpg`, `.jpeg`)
- HEIC (`.heic`) - iPhone Live Photos含む
- PNG (`.png`)

### 動画
- MP4 (`.mp4`) - iPhone/Android共通
- MOV (`.mov`) - iPhone標準
- 3GP (`.3gp`) - Android（古い機種）
- M4V (`.m4v`) - iPhone（まれ）

### 対象外
- 上記以外の形式は警告を出してスキップ
- 将来のバージョンで拡張可能

---

## 6. データ管理方針

### ファイルシステム中心
- ファイル名に撮影日時を持たせる
- 年ごとのフォルダに整理
- DBが消えてもファイル名から再構築可能

### データベース（将来用）
- v1.0では**使用しない**
- v2.0以降でタグ付け・閲覧機能のために導入
- SQLiteを想定
- ファイルスキャンで自動生成可能な設計

---

## 7. 処理フロー（対話モード）

### 全体フロー
```
起動
 ↓
ロックファイルチェック
 ↓
Phase 1a → 確認 → Phase 1b → 確認 → Phase 1c → 確認
 ↓
Phase 2 → 確認
 ↓
Phase 3 → 確認
 ↓
Phase 4（ユーザー確認期間）
 ↓
Phase 5a → 確認
 ↓
Phase 5b（オプション） → 確認
 ↓
終了（ロックファイル削除）
```

### Phase 1a: 軽量スキャン（メタデータ抽出）
**目的**: 全体像の把握、高速で完了

```
処理内容:
1. source/ フォルダを再帰的にスキャン
2. 対応形式のファイルのみ抽出
3. 各ファイルのメタデータを取得:
   - EXIF/動画メタデータから撮影日時
   - ファイルサイズ、拡張子
   - カメラ機種、GPS情報（将来用）

4. organized/ を軽量スキャン（2回目以降用）
   - 既存ファイル名リスト作成
   - ファイルサイズマップ作成
   - メタデータ抽出は不要（高速）

出力:
- metadata_report.json: source/ 全ファイルのメタデータ
- existing_files.json: organized/ の既存ファイル情報
- warnings.txt: EXIF読み取り失敗等の警告

処理時間目安:
- source/ 30,000ファイル: 約50分
- organized/ 30,000ファイル: 約10分（軽量スキャン）

対話:
レポートサマリーを表示後、Phase 1b へ進むか確認
```

### Phase 1b: ハッシュ計算（最適化版）
**目的**: 重複検出用、サイズベースで最適化

```
処理内容:
1. metadata_report.json を読み込み
2. ファイルサイズでグループ化
3. サイズフィルタリング:
   - サイズが一意 → ハッシュ計算スキップ
   - サイズが重複 → ハッシュ計算対象
     * source/ 内での重複
     * source/ vs organized/ の重複
4. 対象ファイルのみSHA-256ハッシュ計算
5. チェックポイント保存（100件ごと）
6. 中断・再開対応

出力:
- hash_report.json: ハッシュ値リスト
  * 計算したファイル
  * スキップしたファイル（サイズ一意）
- checkpoint.json: 進捗保存

処理時間目安（最適化版）:
- 30,000ファイル、重複率20%の場合
- ハッシュ計算対象: 6,000ファイル
- 所要時間: 約1.6時間（従来比80%削減）

対話:
進捗表示、完了後に Phase 1c へ進むか確認
```

### Phase 1c: 分析・レポート生成
**目的**: 重複検出、整理計画の基礎データ作成

```
処理内容:
1. metadata + hash を統合
2. 重複ファイル検出:
   a. source/ 内の重複
   b. source/ vs organized/ の重複（既に整理済み）
3. 日付範囲、ファイル数等の統計情報

出力:
- analysis_report.json: 統合レポート
- duplicates.txt: 重複ファイルリスト（種別ごと）

対話:
分析結果サマリー表示、Phase 2 へ進むか確認
```

### Phase 2: 整理計画（Dry-run）
**目的**: 実際の変更前にシミュレーション

```
処理内容:
1. analysis_report.json を読み込み
2. 新しいファイル名・パスを計算
3. organized/ の既存ファイルを考慮して連番決定
4. ファイル名衝突チェック
5. 重複ファイルの扱い決定:
   - source/ vs organized/ 重複 → スキップ
6. ディスク容量チェック

出力:
- organize_plan.txt: 移動計画の詳細
  * source → organized のマッピング
  * スキップするファイル（重複等）
  * 警告・エラー
  * 必要なディスク容量

対話:
計画内容を表示、ユーザー確認、Phase 3 へ進むか確認
```

### Phase 3: 整理実行
**目的**: 実際のファイル操作

```
処理内容:
1. organized/ 内に年ごとのフォルダ作成
2. ファイルをコピー（移動ではなく安全性重視）
3. バッチ処理（50-100件ずつ）
4. 各バッチで:
   - NAS接続確認
   - ファイルコピー（リトライ付き）
   - コピー後にハッシュ検証
   - チェックポイント保存
5. プログレスバー表示

出力:
- organize_log.json: 処理結果詳細
  * 成功/失敗リスト
  * ハッシュ検証結果
- success.txt / failed.txt

処理時間目安:
- ネットワーク帯域依存
- 100GB, 1Gbps LANで約15-30分

対話:
実行前の最終確認、完了後に Phase 4 の説明
```

### Phase 4: 確認期間
**目的**: ユーザーによる手動確認

```
プログラムの指示:
1. organized/ フォルダを確認してください
2. ランダムにファイルを開いて確認
3. 日付順ソートの確認
4. 問題なければこのプログラムを再実行してPhase 5へ

※ プログラムは一旦終了
※ ユーザーが確認後、再実行して Phase 5 へ進む
```

### Phase 5a: オリジナルファイル削除
**目的**: source/ のクリーンアップ

```
処理内容:
1. organize_log.json を参照
2. status="success" かつ hash_verified=true のファイルのみ対象
3. 削除前に最終確認プロンプト
4. 削除実行
5. 削除ログ出力

対話:
削除対象ファイル数表示、最終確認、実行後に Phase 5b の案内
```

### Phase 5b: 重複ファイル削除（オプション）
**目的**: 重複ファイルのクリーンアップ

```
処理内容:
1. duplicates.txt を参照
2. 各重複グループを表示
3. ユーザーが残すファイルを選択
   または最初のファイルを自動で残す
4. 削除実行

対話:
重複グループごとに確認、削除実行
```

---

## 8. ネットワーク対策

### 課題
- PCからNAS（SMB接続）での長時間処理
- 接続タイムアウトのリスク
- 数万ファイルで数時間かかる

### 対策

#### 1. チェックポイント機能
```json
// checkpoint.json
{
  "phase": "1b_hash_calculation",
  "total_files": 25000,
  "processed_count": 15000,
  "last_updated": "2025-01-16T12:30:45",
  "processed_files": {
    "source/IMG_1234.jpg": {
      "hash": "abc123...",
      "processed_at": "2025-01-16T10:05:23"
    }
  }
}
```
- N件ごとに進捗保存（デフォルト100件）
- 中断しても続きから再開可能

#### 2. リトライ機構
```
- 最大リトライ回数: 3回
- リトライ間隔: 5秒（exponential backoff）
- 対象エラー: OSError, IOError, TimeoutError, PermissionError
```

#### 3. バッチ処理
```
- バッチサイズ: 50-100件
- バッチ間で接続確認
- 5バッチごとにNAS接続チェック
```

#### 4. ローカルキャッシュ
```
- レポート、チェックポイントはPCローカルに保存
- NASへの書き込みを最小化
```

---

## 9. 二重起動防止（ロックファイル機構）

### 基本方針
```
配置場所: organized/.photo-organizer.lock

処理開始時:
- ロックファイルが存在する → チェック処理
- 存在しない → ロックファイル作成、処理開始

処理終了時:
- 正常終了 → ロックファイル削除
- 異常終了（クラッシュ） → ロックファイル残る
```

### ロックファイルの内容
```json
{
  "started_at": "2025-01-16T10:30:00",
  "pc_name": "DESKTOP-ABC123",
  "current_phase": "phase1b_hash"
}
```

### ロックチェックのロジック
```
起動時:
1. organized/.photo-organizer.lock の存在確認

2. 存在しない場合:
   → ロックファイル作成、処理開始

3. 存在する場合:
   a. started_at が24時間以上前？
      → 「古いロックです。削除して続行しますか？ [y/n]」
      → y: 削除して続行、n: 終了
   
   b. 24時間以内:
      → エラーメッセージ表示
      → 手動削除を促して終了

処理中:
- 各Phase開始時に current_phase を更新

終了時:
- ロックファイル削除
```

### エラーメッセージ
```
エラー: 既に処理が実行中、または前回異常終了した可能性があります

ロックファイル: Z:/Photos/organized/.photo-organizer.lock
開始時刻: 2025-01-16 10:30:00
実行PC: DESKTOP-ABC123
フェーズ: phase1b_hash

【対処方法】
1. 他のPCやプロセスで実行していないか確認
2. 実行していない場合は、上記ロックファイルを手動削除してください
3. 再度このプログラムを実行してください
```

---

## 10. 設定ファイル（config.json）

```json
{
  "paths": {
    "source_dir": "Z:/Photos/source",
    "organized_dir": "Z:/Photos/organized",
    "workspace_dir": "C:/photo-organizer"
  },
  
  "network": {
    "retry_count": 3,
    "retry_delay": 5,
    "connection_timeout": 300,
    "batch_size": 50,
    "checkpoint_interval": 100,
    "connection_check_interval": 250
  },
  
  "performance": {
    "hash_buffer_size": 65536,
    "parallel_hash": false,
    "low_priority_mode": true
  },
  
  "hash_optimization": {
    "skip_unique_sizes": true,
    "min_duplicate_count": 2
  },
  
  "incremental_mode": {
    "scan_existing": true,
    "skip_duplicates": true,
    "continue_sequence": true,
    "check_cross_folder": true
  },
  
  "supported_extensions": {
    "image": [".jpg", ".jpeg", ".heic", ".png"],
    "video": [".mp4", ".mov", ".3gp", ".m4v"]
  },
  
  "filename_pattern": "YYYYMMDDhhmmss-NN",
  
  "lock": {
    "auto_delete_hours": 24
  }
}
```

---

## 11. 必要なPythonライブラリ

```txt
Pillow              # JPEG, PNG の EXIF
pillow-heif         # HEIC対応
ffmpeg-python       # 動画メタデータ
tqdm                # プログレスバー
```

標準ライブラリ:
- `hashlib` (SHA-256)
- `pathlib` (パス操作)
- `json` (レポート)
- `logging` (ログ)
- `socket` (PC名取得)

---

## 12. v1.0 の制限事項

### 機能制限
- ✅ **単一ソースディレクトリのみ対応**
  - 複数ソースを処理したい場合は、config.jsonを書き換えて複数回実行
- ✅ **データベース未使用**
  - タグ付け・閲覧機能はv2.0以降
- ✅ **並列処理なし**
  - NAS負荷を考慮してシングルスレッド
- ✅ **対話モード専用**
  - コマンドライン引数での一括実行は未対応
  
### 将来の拡張（v2.0以降）
- プロジェクト管理機能（複数ソース対応）
- インデックスファイルによる高速化
- SQLiteデータベース導入
- Web閲覧インターフェース
- AIタグ付け機能
- サムネイル生成
- 並列処理オプション

---

## 13. 処理時間の見積もり

### 想定: 30,000ファイル、平均2MB/ファイル、重複率20%

| Phase | 処理内容 | 所要時間 |
|-------|---------|---------|
| 1a | メタデータ抽出（source + organized軽量） | 約60分 |
| 1b | ハッシュ計算（最適化版、6,000ファイル） | 約1.6時間 |
| 1c | 分析 | 数分 |
| 2 | Dry-run | 数分 |
| 3 | コピー実行 | 15-30分 |
| 5 | クリーンアップ | 数分 |

**合計**: 約3時間（従来の9時間から大幅短縮）

---

## 14. 安全性への配慮

### ファイル保護
1. **Phase 3ではコピー**（移動ではない）
2. **コピー後のハッシュ検証**
3. **Phase 5で初めて削除**（確認後）

### エラー処理
- 全フェーズでエラーログ出力
- 失敗したファイルのリスト作成
- リトライ機構

### 確認ポイント
- 各Phaseの完了後に確認プロンプト
- Phase 4: 整理結果の手動確認期間
- Phase 5: 削除前の最終確認

---

## 15. 運用フロー

### 初回実行
```
1. ユーザーが source/ にファイル配置
2. プログラム実行
3. Phase 1a → 1b → 1c → 2 → 3
4. Phase 4（ユーザー確認）
5. プログラム再実行
6. Phase 5a（source/ 削除）
7. Phase 5b（重複削除、オプション）
```

### 2回目以降
```
1. ユーザーが新しいファイルを source/ に配置
2. プログラム実行
3. organized/ をスキャン（既存ファイル確認）
4. 重複チェック（source vs organized）
5. 連番を既存から継続
6. 以降は初回と同じフロー
```

---

## 16. エラー処理方針

### 回復可能なエラー
- ネットワーク一時切断 → リトライ
- ファイル読み取り失敗 → スキップしてログ記録
- EXIF読み取り失敗 → ファイル作成日時を使用

### 回復不可能なエラー
- NAS接続不可（リトライ後も失敗） → エラー終了
- ディスク容量不足 → Phase 2 で事前検出
- 設定ファイル不正 → 起動時にエラー

### クラッシュ時の対応
- チェックポイントから再開
- ロックファイルは手動削除（24時間以降は自動削除確認）
